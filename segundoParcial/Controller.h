int controller_loadFromText(char*, LinkedList*);
int controller_saveFromText(char*, LinkedList*);

void mostrarLista(LinkedList*);

int calcularPromedio(void*);
int llamadoProm(LinkedList*);

int calcularCant(void*,int);
int llamadoCant(LinkedList*);

void mostrarConProm(LinkedList*);
